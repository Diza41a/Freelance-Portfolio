import { styled } from '@mui/material';

export const classNames = {
  root: 'Landing',
};

const LandingPage = styled('div')(() => ({
  position: 'relative',
  overflowX: 'hidden',
}));

export default {
  LandingPage,
};
